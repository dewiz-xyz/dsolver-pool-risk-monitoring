apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Prometheus port set via --web.listen-address in systemd service override
    url: http://localhost:9000
    isDefault: true
    editable: false
