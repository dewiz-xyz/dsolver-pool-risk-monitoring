apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Prometheus is running natively on port 9000
    url: http://localhost:9000
    isDefault: true
    editable: false
