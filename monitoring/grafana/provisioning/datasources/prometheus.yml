apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Prometheus default port (no custom --web.listen-address set)
    url: http://localhost:9090
    isDefault: true
    editable: false
